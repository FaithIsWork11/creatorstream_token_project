{
  "name": "creatorstream-token",
  "version": "1.0.0",
  "scripts": {
    "test": "hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network stratos"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}